class Solution(object):
    def missingNumber(self, nums):
    
        s2 = 0
        n = len(nums)
        s1 = int((n * (n + 1)) / 2)
        for j in nums:
            s2 += j
        return s1 - s2
